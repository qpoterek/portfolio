---
const { 
	language, 
	flag, 
	level, 
	level_oral, 
	level_written,
} = Astro.props
---

<div class="
	flex gap-x-4 h-fit w-fit px-4 py-3 
	rounded bg-white/50 dark:bg-neutral-700/50 
">
	<img src={flag} class="pointer-events-none h-10 rounded" loading="lazy"/>
	<div class="flex flex-col justify-center">
		<h3 class="text-sm font-bold">{language}</h3>
		<div class="text-xs text-neutral-500 dark:text-neutral-400">
			{ level && <p>{level}</p> }
			{ 
				!level && 
				<div class="flex flex-wrap gap-x-2">
					<p>💬 Parlé {level_oral}</p>
					<p>✒️ Ecrit {level_written}</p>
				</div>
			}
		</div>
	</div>
</div>