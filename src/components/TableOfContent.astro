---
const { contents } = Astro.props
---

<section class="flex flex-col gap-y-12">
	<h2 class="text-2xl font-bold">📑 Table des matières</h2>
	{
		contents.map(content => (
			<a href={`#${content.id}`}>{content.title}</a>
		))
	}
</section>